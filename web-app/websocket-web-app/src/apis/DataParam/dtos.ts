export type loginUserDto = {
    email: string;
    password: string;
}

export type addUserDto = {
    nickName: string;
    fullName: string;
    email: string;
    password: string;
}

export type logoutUserDto = {
    email: string;
    password: string;
}